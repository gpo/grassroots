{
  "name": "generate-metadata",
  "version": "1.0.0",
  "description": "",
  "main": "GenerateMetadata.js",
  "type": "module",
  "scripts": {
    "gen-metadata": "node dist/GenerateMetadata.js",
    "gen-metadata-watch": "node dist/GenerateMetadata.js --watch",
    "build": "swc src --strip-leading-paths --out-dir dist",
    "build-watch": "swc src --strip-leading-paths --out-dir dist --watch",
    "clean": "rm -f ../grassroots-shared/src/metadata.ts && rm -rf dist && rm -rf .turbo && rm -rf /app/.turbo"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.11.0",
  "dependencies": {
    "@nestjs/cli": "^11.0.10",
    "@nestjs/swagger": "^11.2.0",
    "@swc/cli": "^0.6.0",
    "@swc/core": "^1.13.5",
    "@types/node": "^24.3.2"
  },
  "devDependencies": {
    "chokidar": "^4.0.3",
    "chokidar-cli": "^3.0.0"
  }
}

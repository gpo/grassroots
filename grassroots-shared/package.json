{
  "name": "grassroots-shared",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "swc src --strip-leading-paths --out-dir dist --config-file=../.swcrc",
    "build-watch": "swc src --strip-leading-paths --out-dir dist --config-file=../.swcrc",
    "tsc": "tsc",
    "tsc-watch": "tsc --watch",
    "clean": "rm -rf .turbo && rm -rf dist && rm -f *.tsbuildinfo && rm -rf /app/.turbo"
  },
  "keywords": [],
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.11.0",
  "type": "module",
  "composite": true,
  "exports": {
    "./*": {
      "require": "./dist/*.js",
      "import": "./dist/*.js"
    }
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/swagger": "^11.2.0",
    "@types/node": "^24.3.2",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "generate-metadata": "workspace:*",
    "openapi-fetch": "^0.13.8",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@swc/cli": "^0.6.0",
    "@swc/core": "^1.13.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}

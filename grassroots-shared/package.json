{
  "name": "grassroots-shared",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "rm -rf dist && tsc --emitDeclarationOnly -p tsconfig.nometadata.json && tsc --emitDeclarationOnly -p tsconfig.justmetadata.json && swc src --strip-leading-paths --out-dir dist",
    "clean": "rm -rf dist && rm -f tsconfig.tsbuildinfo"
  },
  "keywords": [],
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.11.0",
  "type": "module",
  "composite": true,
  "exports": {
    "./*": {
      "require": "./dist/*.js",
      "import": "./dist/*.js",
      "types": "./dist/*.d.ts"
    }
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/swagger": "^11.2.0",
    "@types/node": "^24.3.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "generate-metadata": "workspace:*",
    "openapi-fetch": "^0.13.8",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@swc/cli": "^0.6.0",
    "@swc/core": "^1.13.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}

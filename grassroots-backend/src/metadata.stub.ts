/* eslint-disable check-file/filename-naming-convention */
/**
 * It is a stub file to satisfy the TypeScript compiler.
 */
export default (): Promise<Record<string, unknown>> => {
  return Promise.resolve({});
};

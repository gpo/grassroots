name: grassroots

services:
  db:
    image: postgres
    container_name: db
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    ports:
      - 5432:5432
    env_file:
      - ../dev.env
{
  "name": "casl-subjects",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "exports": {
    "./*": {
      "require": "./dist/*.js",
      "types": "./dist/*.d.ts"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gen-only": "tsc && node --enable-source-maps dist/GenerateSubjects.js",
    "build-only": "tsc --emitDeclarationOnly -p tsconfig-generated.json && swc --strip-leading-paths gen --out-dir dist-gen",
    "gen": "pnpm run gen-only && pnpm run build-only"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {
    "@types/node": "^24.3.0",
    "ts-morph": "^26.0.0",
    "typescript": "^5.9.2"
  }
}
